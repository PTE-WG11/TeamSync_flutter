# TeamSync 开发准备清单

## 📋 文档信息
- **项目名称**: TeamSync 团队协作管理系统
- **版本**: v1.0
- **日期**: 2026-02-10

---

## 1. 项目文档汇总

| 序号 | 文档名称 | 文件路径 | 说明 |
|------|---------|---------|------|
| 1 | 功能模块拆解 | `docs/01_功能模块拆解.md` | 8个核心模块详细设计 |
| 2 | 用户旅程图 | `docs/02_用户旅程图.md` | 四种角色的完整用户旅程 |
| 3 | REST API接口预定义 | `docs/03_REST_API接口预定义.md` | 60+ API端点设计 |
| 4 | 前端开发指南 | `docs/04_前端开发指南.md` | Flutter Web 开发规范 |
| 5 | 设计系统规范 | `docs/05_设计系统规范.md` | 颜色/字体/间距规范 |
| 6 | 组件库规范 | `docs/06_组件库规范.md` | 基础组件Flutter实现 |
| 7 | SVG图标规范 | `docs/07_SVG图标规范.md` | 30+ SVG图标代码 |
| 8 | 动画与交互规范 | `docs/08_动画与交互规范.md` | 动画时长/缓动/交互 |
| 9 | 页面实现规范 | `docs/09_页面实现规范.md` | 登录/注册/首页实现 |
| 10 | 开发准备清单 | `docs/10_开发准备清单.md` | 本文件 |

---

## 2. 开发环境准备

### 2.1 Flutter 环境

```bash
# 检查 Flutter 版本 (需要 3.16+)
flutter --version

# 启用 Web 支持
flutter config --enable-web

# 检查环境
flutter doctor
```

### 2.2 项目初始化

```bash
# 创建项目
flutter create teamsync --platforms web

# 进入项目目录
cd teamsync

# 添加依赖
cat > dependencies.txt << EOF
# 状态管理
flutter_bloc: ^8.1.3

# 路由
go_router: ^13.0.1

# 依赖注入
get_it: ^7.6.4
injectable: ^2.3.2

# 网络请求
dio: ^5.4.0
retrofit: ^4.0.3

# 数据类
equatable: ^2.0.5
freezed_annotation: ^2.4.1
json_annotation: ^4.8.1

# SVG 图标
flutter_svg: ^2.0.9

# 本地存储
shared_preferences: ^2.2.2

# WebSocket
web_socket_channel: ^2.4.0

# 日期处理
intl: ^0.18.1

# 图表（甘特图/看板）
flutter_gantt_chart: ^0.0.4

# 开发依赖
dev_dependencies:
build_runner: ^2.4.8
freezed: ^2.4.6
json_serializable: ^6.7.1
retrofit_generator: ^8.0.6
injectable_generator: ^2.4.1
EOF
```

---

## 3. 资源文件清单

### 3.1 图标资源 (assets/icons/)

```
assets/
└── icons/
    ├── navigation/
    │   ├── home.svg
    │   ├── project.svg
    │   ├── calendar.svg
    │   ├── settings.svg
    │   └── logout.svg
    ├── task/
    │   ├── task.svg
    │   ├── check.svg
    │   ├── clock.svg
    │   └── flag.svg
    ├── status/
    │   ├── planning.svg
    │   ├── pending.svg
    │   ├── in_progress.svg
    │   ├── completed.svg
    │   └── overdue.svg
    ├── action/
    │   ├── add.svg
    │   ├── edit.svg
    │   ├── delete.svg
    │   ├── search.svg
    │   ├── filter.svg
    │   ├── sort.svg
    │   ├── more.svg
    │   └── close.svg
    ├── file/
    │   ├── attachment.svg
    │   ├── upload.svg
    │   ├── download.svg
    │   ├── file.svg
    │   └── image.svg
    ├── user/
    │   ├── user.svg
    │   ├── team.svg
    │   ├── invite.svg
    │   └── role.svg
    └── feedback/
        ├── success.svg
        ├── error.svg
        ├── warning.svg
        ├── info.svg
        └── loading.svg
```

### 3.2 pubspec.yaml 配置

```yaml
name: teamsync
description: TeamSync 团队协作管理系统

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 状态管理
  flutter_bloc: ^8.1.3
  
  # 路由
  go_router: ^13.0.1
  
  # 依赖注入
  get_it: ^7.6.4
  injectable: ^2.3.2
  
  # 网络请求
  dio: ^5.4.0
  retrofit: ^4.0.3
  
  # 数据类
  equatable: ^2.0.5
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  
  # SVG 图标
  flutter_svg: ^2.0.9
  
  # 本地存储
  shared_preferences: ^2.2.2
  
  # WebSocket
  web_socket_channel: ^2.4.0
  
  # 日期处理
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  retrofit_generator: ^8.0.6
  injectable_generator: ^2.4.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/navigation/
    - assets/icons/task/
    - assets/icons/status/
    - assets/icons/action/
    - assets/icons/file/
    - assets/icons/user/
    - assets/icons/feedback/
```

---

## 4. 代码生成命令

```bash
# 生成 Freezed 数据类
flutter pub run build_runner build --delete-conflicting-outputs

# 监视模式（开发时使用）
flutter pub run build_runner watch --delete-conflicting-outputs

# 生成依赖注入
flutter pub run injectable_generator:injectable_generator build
```

---

## 5. 开发检查清单

### Phase 1: 基础架构

- [ ] Flutter 项目初始化
- [ ] 依赖安装
- [ ] 目录结构创建
- [ ] 主题配置 (颜色/字体/间距)
- [ ] 路由配置
- [ ] 依赖注入配置
- [ ] API 客户端配置

### Phase 2: 基础组件

- [ ] AppButton (按钮)
- [ ] AppTextField (输入框)
- [ ] AppCheckbox (复选框)
- [ ] StatusBadge (状态徽章)
- [ ] PriorityBadge (优先级徽章)
- [ ] StatCard (统计卡片)
- [ ] TaskCard (任务卡片)
- [ ] Sidebar (侧边栏)
- [ ] AppToast (提示)
- [ ] AppLoading (加载)

### Phase 3: SVG 图标

- [ ] 创建 assets/icons/ 目录
- [ ] 复制所有 SVG 文件
- [ ] 配置 pubspec.yaml
- [ ] 创建 AppIcon 组件
- [ ] 测试图标显示

### Phase 4: 页面实现

- [ ] 登录页
- [ ] 注册页
- [ ] 成员首页
- [ ] 项目列表页
- [ ] 任务详情页

### Phase 5: 功能集成

- [ ] 登录/注册 API 对接
- [ ] Token 管理
- [ ] 路由守卫
- [ ] 错误处理
- [ ] 加载状态

---

## 6. 运行命令

```bash
# 开发模式运行
flutter run -d chrome

# 构建 Web 生产版本
flutter build web --release

# 构建并指定基础路径
flutter build web --release --base-href "/teamsync/"

# 启用 Web 渲染器
flutter run -d chrome --web-renderer html
flutter run -d chrome --web-renderer canvaskit
```

---

## 7. 文件结构检查

```
lib/
├── main.dart
├── app.dart
├── injection.dart
├── config/
│   ├── constants.dart
│   ├── theme.dart
│   ├── routes.dart
│   └── animations.dart
├── core/
│   ├── errors/
│   ├── network/
│   └── utils/
├── features/
│   ├── auth/
│   ├── project/
│   ├── task/
│   ├── dashboard/
│   ├── member/
│   ├── notification/
│   └── file/
├── shared/
│   ├── widgets/
│   ├── models/
│   └── extensions/
└── assets/
    └── icons/
```

---

## 8. 快速开始

```bash
# 1. 克隆或创建项目
git clone <repository> teamsync
# 或: flutter create teamsync --platforms web

# 2. 安装依赖
cd teamsync
flutter pub get

# 3. 复制 SVG 图标
mkdir -p assets/icons
cp -r <design_assets>/icons/* assets/icons/

# 4. 生成代码
flutter pub run build_runner build --delete-conflicting-outputs

# 5. 运行项目
flutter run -d chrome
```

---

*文档版本: v1.0 | 最后更新: 2026-02-10*
